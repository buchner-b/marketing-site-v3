/**
 * UI primitives — Button, Card, etc.
 *
 * All components should:
 * - Use native HTML elements first (<button>, <a>, <details>)
 * - Support 44x44px min touch targets
 * - Accept className prop for Tailwind composition via cn()
 */
export { };
