/**
 * data/
 *
 * All hardcoded content lives here in TypeScript files.
 * Use `satisfies` for type safety on data objects.
 *
 * Example:
 * ```ts
 * import type { PageData } from "@/types";
 *
 * export const homeData = {
 *   hero: { title: "...", subtitle: "..." },
 * } satisfies PageData;
 * ```
 */
export { };
